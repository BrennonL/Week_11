{erl_opts, [no_debug_info]}.
{deps, []}.

{escript_incl_apps,
 [trie]}.
{escript_main_app, trie}.
{escript_name, trie}.
{escript_emu_args, "%%! +sbtu +A1\n"}.

%% Profiles
{profiles, [{test,
             [{erl_opts, [debug_info]}
            ]}]}.
